# StreamlineVPN Configuration

# This is a Jinja2 template example for generating application configuration.
# Customize values and structure per deployment needs.

# Example configuration:
log_level: {{ log_level | default('INFO') }}
redis_url: {{ redis_url | default('redis://localhost:6379/0') }}
postgres_url: {{ postgres_url | default('postgresql://user:password@localhost:5432/streamline') }}
