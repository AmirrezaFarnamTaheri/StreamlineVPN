apiVersion: v1
kind: Service
metadata:
  name: {{ include "streamline-vpn.fullname" . }}
  labels:
    {{- include "streamline-vpn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "streamline-vpn.selectorLabels" . | nindent 4 }}
