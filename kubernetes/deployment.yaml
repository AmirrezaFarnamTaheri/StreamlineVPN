apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamline-vpn
  namespace: streamline
  labels:
    app: streamline-vpn
    version: v2.0.0
spec:
  replicas: 3
  selector:
    matchLabels:
      app: streamline-vpn
  template:
    metadata:
      labels:
        app: streamline-vpn
        version: v2.0.0
    spec:
      containers:
      - name: streamline-vpn
        image: streamlinevpn/streamlinevpn:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: STREAMLINE_ENV
          value: "production"
        - name: STREAMLINE_LOG_LEVEL
          value: "INFO"
        - name: STREAMLINE_REDIS_URL
          value: "redis://redis-service:6379/0"
        - name: STREAMLINE_DB_URL
          valueFrom:
            secretKeyRef:
              name: streamline-secrets
              key: db-url
        - name: STREAMLINE_API_KEY
          valueFrom:
            secretKeyRef:
              name: streamline-secrets
              key: api-key
        - name: STREAMLINE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: streamline-secrets
              key: secret-key
        resources:
          limits:
            cpu: "2"
            memory: "2Gi"
          requests:
            cpu: "1"
            memory: "1Gi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: output
          mountPath: /app/output
      volumes:
      - name: config
        configMap:
          name: streamline-config
      - name: output
        persistentVolumeClaim:
          claimName: streamline-output-pvc
